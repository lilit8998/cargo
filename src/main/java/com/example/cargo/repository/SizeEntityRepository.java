package com.example.cargo.repository;

import com.example.cargo.entity.SizeEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SizeEntityRepository extends JpaRepository<SizeEntity,Integer> {
}
